diff --git a/node_modules/react-native-infinite-pager/lib/typescript/index.d.ts b/node_modules/react-native-infinite-pager/lib/typescript/index.d.ts
index 8c3c485..26e0237 100644
--- a/node_modules/react-native-infinite-pager/lib/typescript/index.d.ts
+++ b/node_modules/react-native-infinite-pager/lib/typescript/index.d.ts
@@ -45,6 +45,7 @@ export type InfinitePagerProps = {
     height?: number;
     minDistance?: number;
     initialIndex?: number;
+    followCrossAxis?: boolean;
 };
 type ImperativeApiOptions = {
     animated?: boolean;
diff --git a/node_modules/react-native-infinite-pager/src/index.tsx b/node_modules/react-native-infinite-pager/src/index.tsx
index e0bfa4a..bef1457 100644
--- a/node_modules/react-native-infinite-pager/src/index.tsx
+++ b/node_modules/react-native-infinite-pager/src/index.tsx
@@ -103,6 +103,11 @@ export type InfinitePagerProps = {
   height?: number;
   minDistance?: number;
   initialIndex?: number;
+  // Keep following the finger along the paging axis while it also drifts across
+  // it. By default a drag that is mostly across the axis is ignored, which is
+  // right when the pages scroll that way themselves and wrong when they do not:
+  // a slow drag with a little vertical wobble would freeze until release.
+  followCrossAxis?: boolean;
 };
 
 type ImperativeApiOptions = {
@@ -148,6 +153,7 @@ function InfinitePager(
     minDistance,
     initialIndex = 0,
     syncNode,
+    followCrossAxis = false,
   }: InfinitePagerProps,
   ref: React.ForwardedRef<InfinitePagerImperativeApi>
 ) {
@@ -373,6 +379,7 @@ function InfinitePager(
             : evt.translationY;
 
           const isSwipingCrossAxis =
+            !followCrossAxis &&
             Math.abs(crossAxisTranslate) > 10 &&
             Math.abs(crossAxisTranslate) > Math.abs(evtTranslate);
 
@@ -412,6 +419,7 @@ function InfinitePager(
             ? evt.translationX
             : evt.translationY;
           const isSwipingCrossAxis =
+            !followCrossAxis &&
             Math.abs(crossAxisTranslate) > Math.abs(evtTranslate);
 
           const isFling =
@@ -467,6 +475,7 @@ function InfinitePager(
       bouncePct,
       debugTag,
       flingVelocity,
+      followCrossAxis,
       gesturesDisabledAnim,
       initTouchX,
       initTouchY,
